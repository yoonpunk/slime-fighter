# ⚔️ 과제: 슬라임 군단 격퇴기 (Slime Fighter)

본 프로젝트는 자바의 기초 문법을 넘어 **객체지향 설계(OOP)**와 **컬렉션 프레임워크(List)**를 실제 게임 로직에 적용해 보는 실습 과제입니다.

---

## 1. 프로젝트 개요
용사가 되어 마을을 습격한 **N마리의 슬라임 군단**을 물리치는 CLI(Command Line Interface) 기반 턴제 RPG 게임을 구현합니다.

* **언어:** Java (JDK 17 이상 권장)
* **환경:** 콘솔(Console) 입력 및 출력
* **핵심 키워드:** 클래스와 객체, 생성자, `ArrayList`, `Scanner`, `Random`, 조건문 및 반복문

---

## 2. 요구 사항 (Functional Requirements)

### ① 캐릭터 설계
1. **용사(Hero) 클래스**
    * **속성:** 이름, 현재 체력(HP), 최대 체력, 공격력, 포션 개수(초기 3개)
    * **기능:**
        * `attack(Slime target)`: 선택한 슬라임에게 공격력만큼 데미지를 입힘 (20% 확률로 2배의 치명타 발생).
        * `drinkPotion()`: 포션을 사용해 체력을 30 회복 (최대 체력을 초과할 수 없음).
        * `takeDamage(int damage)`: 전달받은 데미지만큼 현재 체력을 감소시킴.
        * `isAlive()`: 현재 체력이 0보다 큰지 확인.
2. **슬라임(Slime) 클래스**
    * **속성:** 이름(슬라임1, 슬라임2...), 현재 체력(HP), 공격력
    * **기능:**
        * `attack(Hero target)`: 용사에게 `공격력` ~ `공격력 + 5` 사이의 랜덤한 데미지를 입힘.
        * `isAlive()`: 현재 체력이 0보다 큰지 확인.
        * `takeDamage(int damage)`: 전달받은 데미지만큼 현재 체력을 감소시킴.

### ② 게임 흐름
1. **준비:** 프로그램 시작 시 용사의 이름을 입력받고, 슬라임 3마리를 생성하여 `ArrayList<Slime>`에 저장합니다.
2. **전투 루프:** 용사 혹은 모든 슬라임이 전멸할 때까지 다음을 반복합니다.
    * **상태 출력:** 현재 용사의 상태와 살아있는 모든 슬라임의 목록(이름/HP)을 보여줍니다.
    * **용사 턴:** 사용자는 `1.공격`, `2.회복` 중 하나를 선택합니다.
        * 공격 선택 시, 살아있는 슬라임 중 공격할 대상을 번호(인덱스)로 선택합니다.
    * **슬라임 턴:** 살아있는 모든 슬라임이 차례대로 용사를 공격합니다.
3. **결과:** 승리(모든 슬라임 처치) 또는 패배(용사 사망) 메시지를 출력하고 프로그램을 종료합니다.

---

## 3. 기술적 제약 사항 (Technical Constraints)

* **캡슐화:** 모든 필드(속성)는 `private`으로 선언하고, 필요한 경우 `Getter/Setter`를 구현하세요.
* **컬렉션 활용:** 슬라임 목록은 반드시 `ArrayList<Slime>`을 사용하여 관리하세요.
* **객체 간 상호작용:** 용사가 슬라임을 공격할 때, 슬라임의 HP를 직접 수정하지 말고 슬라임 객체의 메서드를 호출하여 상호작용하세요.

---

## 4. 입출력 예시 (Interface)

```text
========================================
    🏰 슬라임 군단이 마을을 습격했습니다!
========================================
[시스템] 용사의 이름을 입력하세요: 지오

[시스템] '지오' 용사님, 슬라임 3마리와의 전투를 시작합니다!

----------------------------------------
[용사] 지오 (HP: 100/100) | 포션: 3개
[적들]
1. 초록 슬라임 (HP: 50/50)
2. 파란 슬라임 (HP: 50/50)
3. 빨간 슬라임 (HP: 50/50)
----------------------------------------
[행동 선택] 1.공격  2.회복
입력: 1

[시스템] 몇 번 슬라임을 공격하시겠습니까?
입력: 2

>> [전투] 지오의 공격! '파란 슬라임'에게 20의 데미지를 입혔습니다.

>> [반격] 살아남은 슬라임들의 공격!
>> 초록 슬라임이 8의 데미지를 입혔습니다.
>> 파란 슬라임이 8의 데미지를 입혔습니다.
>> 빨간 슬라임이 8의 데미지를 입혔습니다.

----------------------------------------
[용사] 지오 (HP: 76/100) | 포션: 3개
[적들]
1. 초록 슬라임 (HP: 50/50)
2. 파란 슬라임 (HP: 30/50)
3. 빨간 슬라임 (HP: 50/50)
----------------------------------------
... (전투 반복) ...

>> [안내] 파란 슬라임을 처치했습니다!

>> [결과] 모든 슬라임을 물리쳤습니다! 마을에 평화가 찾아왔습니다.
========================================
```

## 5. 예시코드
1. Hero.java (용사 클래스)  
   객체의 상태를 관리하고, 행동(메서드)을 정의하는 연습을 합니다.
```java
public class Hero {
    private String name;
    private int hp;
    private int maxHp;
    private int atk;
    private int potionCount;

    public Hero(String name) {
        this.name = name;
        this.maxHp = 100;
        this.hp = 100;
        this.atk = 20;
        this.potionCount = 3;
    }

    public void attack(Slime target) {
        System.out.println(this.name + "의 공격!");
        // TODO: 20% 확률로 치명타(데미지 2배) 로직을 구현해보세요.
        // TODO: target.takeDamage(finalAtk)를 호출하세요.
    }

    public void takeDamage(int damage) {
        this.hp -= damage;
        if (this.hp < 0) this.hp = 0;
        System.out.println(this.name + "이(가) " + damage + "의 데미지를 입었습니다. (남은 HP: " + this.hp + ")");
    }

    public void drinkPotion() {
        // TODO: 포션 개수를 체크하고, HP를 30 회복하는 로직을 구현해보세요.
        // (단, maxHp를 초과할 수 없습니다.)
    }

    public boolean isAlive() {
        return this.hp > 0;
    }

    // Getter들 (이름, HP, 포션 개수 출력용)
    public String getName() { return name; }
    public int getHp() { return hp; }
    public int getPotionCount() { return potionCount; }
}
```

2. Slime.java (슬라임 클래스)  
   여러 마리가 생성될 것이므로, 이름이 다르게 설정될 수 있도록 설계합니다.

```java
import java.util.Random;

public class Slime {
    private String name;
    private int hp;
    private int atk;

    public Slime(String name) {
        this.name = name;
        this.hp = 50;
        this.atk = 10;
    }

    public void attack(Hero target) {
        if (!isAlive()) return;
        
        Random random = new Random();
        // TODO: atk ~ atk + 5 사이의 랜덤 데미지를 계산하여 target.takeDamage()를 호출하세요.
    }

    public void takeDamage(int damage) {
        this.hp -= damage;
        if (this.hp < 0) this.hp = 0;
        System.out.println(this.name + "이(가) " + damage + "의 데미지를 입었습니다. (남은 HP: " + this.hp + ")");
    }

    public boolean isAlive() {
        return this.hp > 0;
    }

    public String getName() { return name; }
    public int getHp() { return hp; }
}
```
3. Main.java (게임 실행부)
   ArrayList와 전체적인 게임 흐름을 제어하는 메인 로직입니다.

```java
import java.util.ArrayList;
import java.util.List;
import java.util.Scanner;

public class GameMain {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        
        System.out.print("[시스템] 용사의 이름을 입력하세요: ");
        String heroName = scanner.nextLine();
        
        // 용사 & 슬라임 초기화
       
        // 게임 로직 구현 (루프)
        while () {

            // 1. 용사 턴

            // 2. 슬라임 턴
        }

        // 3. 결과 출력
    }

    private static void printStatus(Hero hero, List<Slime> slimes) {
        // TODO: 현재 용사와 슬라임들의 HP 상태를 예쁘게 출력하는 로직을 작성해보세요.
    }

    private static boolean isAnySlimeAlive(List<Slime> slimes) {
        // TODO: 리스트를 순회하며 살아있는 슬라임이 한 마리라도 있는지 확인하는 로직을 작성해보세요.
        return true; // 임시 반환값
    }
}
```

## 6. 보너스 미션 (선택 사항)
- 레벨업: 슬라임을 한 마리 처치할 때마다 용사의 공격력이 5씩 상승합니다.
- 광역 공격: MP를 추가하고, 이를 소모하여 모든 슬라임에게 절반의 데미지를 입히는 스킬을 추가해 보세요.